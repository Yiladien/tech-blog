<div class="container">
  <div class="row">
    <div class="col-1"></div>
    <div class="col-10">
        <ol class="post-list">
          {{#each posts as |post|}}
          <li style="list-style-type:none">
            {{> post-info post}}
          </li>
          {{/each}}
        </ol>
      </div>
    <div class="col-1"></div>
  </div>
</div>